import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { I18nextProvider } from 'react-i18next'; import { beforeAll, describe, expect, it, vi } from 'vitest'; import { i18n, initIafI18n } from '@iaf/i18n'; import { MdmModelCenterPage } from './MdmModelCenterPage';
vi.mock('./hooks',()=>({useMdmModels:()=>({data:[{id:1,domainCode:'manufacturing',code:'material',name:'物料主数据',recordType:'MASTER',versionEnabled:true,effectiveDateEnabled:true,organizationScopeEnabled:true,approvalRequired:true,status:'PUBLISHED',currentModelVersion:1,uiSchema:{},fields:[]}],isLoading:false,isError:false,refetch:vi.fn()}),useCreateMdmModel:()=>({mutate:vi.fn(),isPending:false})}));
describe('MdmModelCenterPage',()=>{beforeAll(async()=>{await initIafI18n();});it('renders metadata model and workspace action',()=>{render(<I18nextProvider i18n={i18n}><MemoryRouter><MdmModelCenterPage/></MemoryRouter></I18nextProvider>);expect(screen.getByText('物料主数据')).toBeInTheDocument();expect(screen.getByRole('button',{name:'打开数据工作台'})).toBeInTheDocument();});});
