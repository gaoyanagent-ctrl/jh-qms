# TASK-0820 MDM 可视化关联校验与失焦反馈

## 目标

将跨对象主数据校验从技术 JSON 配置提升为业务用户可维护的可视化规则，并在表单字段失焦时提供即时反馈。

## 范围

- 可视化配置当前字段、目标模型和多条 AND 匹配条件。
- 配置保存/失焦时机、阻断/警告策略、自定义提示和启停状态。
- 保存继续作为最终校验关口；警告不阻断保存。
- 字段失焦调用只读校验 API，不产生记录或版本。
- 更新 API、测试、规格和代码地图。

## 非范围

- OR 条件分组、缓存、联动修改和外部订阅。
